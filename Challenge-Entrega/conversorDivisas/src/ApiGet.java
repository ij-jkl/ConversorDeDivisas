public record ApiGet(String result,
                     String base_code,
                     String target_code,
                     Double conversion_rate) {
}

// Variables que uso de la api